name: ia_plants
description: "Robot Jardinero — identificación de enfermedades en plantas via IA."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.7   # cámara / galería
  http: ^1.2.2           # llamadas al backend ASP.NET Core
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
